# RecCli Python dependencies

# Install this checkout so `python3 -m reccli...` works after a fresh clone.
-e .

# Core CLI + session system
prompt_toolkit>=3.0.0
tiktoken>=0.5.0
numpy>=1.24.0
blake3>=0.4.0

# Provider clients
anthropic>=0.7.0
openai>=1.0.0

# Structural code parsing for project initialization
tree-sitter
tree-sitter-language-pack

# MCP server
mcp[cli]>=1.20.0

# Development / validation
pytest>=7.4.0
